#include <stdbool.h>
#include <stdlib.h>

// Reverse s, n is length of s
void reverse(char *s, int n);
// returns true if s1 == s2
bool strcmp1(char *s1, char *s2);
// Read a line from stdin, up to maxlen, return length of line
size_t getline1(char *line, size_t maxlen);

